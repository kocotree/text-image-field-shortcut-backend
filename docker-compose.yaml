version: "3.9"

services:
  text-image-field-shortcut:
    image: text-image-field-shortcut:1.2
    container_name: text-image-field-shortcut
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: on-failure